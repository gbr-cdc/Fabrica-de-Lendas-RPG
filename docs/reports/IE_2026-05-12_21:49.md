# IE Report - 2026-05-12 21:49

## Task
BattleManager should add characters at `current_tick + action_cost_base` instead of tick 0, following `[GDD.CORE.TIME.INIT]`.

## Files Modified

### `battle/BattleManager.py`
- Changed `add_character` signature: `start_tick: int = 0` → `start_tick: int | None = None`.
- Added logic: if `start_tick is None`, set `start_tick = self.current_tick + character.action_cost_base`.

### `tests/battle/test_EngineEdgeCases.py`
- `test_battle_manager_move_action_cost`: Updated assertion from `50` to `150`.
  - **Reasoning:** Character (cost=100) now enters at tick 100. After a MOVE_ACTION (cost=50), next tick is `100 + 50 = 150`.

### `.forbidden/architecture.md`
- `[ARCH.TEST_QUALITY.ACTIONS]`: Clarified that `start_tick` should be provided explicitly for tests needing immediate or specific scheduling. Default behavior is `current_tick + action_cost_base`.
- `[ARCH.DOC.battle.BattleManager.BattleManager.add_character]`: Updated signature and step 2 to document the new default scheduling logic referencing `[GDD.CORE.TIME.INIT]`.

## Test Results
- 175 passed, 0 failed.
