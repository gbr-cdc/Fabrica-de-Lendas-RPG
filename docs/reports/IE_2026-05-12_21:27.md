# IE Report — 2026-05-12 21:27

## Objective
Add full formula breakdowns for defense rolls to the battle history stream, mirroring the existing `ATK_CALC` and `DMG_CALC` tags.

## Changes Made

### `core/Events.py`
- Added `HistoryEmitter.def_calc(target_id, roll, rank, bdd, final)` → `DEF_CALC|...`

### `battle/BattleActions.py`
- Emitted `DEF_CALC` after each defense roll resolution (single target and AoE resolution).

### `views/BattleView.py`
- Added `DEF_CALC` → `[CALC] {target} Final Defense: {final} = {roll} (Roll) + {rank} (Rank) + {bdd} (BdD)`

### `tests/battle/test_HistoryCalculations.py`
- Added `TestDefCalcHistory`: verifies `DEF_CALC` tag presence on hit/miss, formula correctness, and ordering (after `ROLL|DEF`).
- Added `test_def_calc_parsed` and `test_def_calc_full_narrative` to `TestBattleViewCalcParsing`.

### Architecture Documentation
- Created `[ARCH.DOC.core.Events.HistoryEmitter.def_calc]`.
- Updated `[ARCH.DOC.battle.BattleActions.AttackAction.execute]` to include `DEF_CALC`.
- Updated `[ARCH.DOC.views.BattleView.BattleView._parse_entry]` to include `DEF_CALC`.

## Test Results
`17 passed in 0.23s` (specifically `test_HistoryCalculations.py`)
All 18 tests in `test_HistoryCalculations.py` pass.
Overall project tests: `172 passed` — all tests pass.
