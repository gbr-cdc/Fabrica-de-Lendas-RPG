# IE Report – 2026-05-12_23:23

## Objective
Implement `add_bda` effect hook and refactor `AttackAction` tests to be hook-isolated rather than data-dependent.

## Changes

### `battle/BattleActions.py`
- Added `_build_add_bda(effect, action)` hook builder.
  - Listens on `on_roll_modify`.
  - Increments `attack_load.bda` by `effect.parameters["amount"]`.
  - Emits `ACTION_HOOK` + `ATK_LOAD|bda` structured events.
- Registered `"add_bda"` in `EFFECT_HOOK_BUILDERS`.

### `tests/battle/test_BattleActions.py`
- **Removed** dependency on `DataManager.get_template("SkillN1")` for effect hook unit tests.
- Added `_make_template(effect_id, params)` helper to build minimal `AttackActionTemplate` in-test.
- Added `_make_attack_load(actor, target)` helper for direct hook invocation.
- Each effect hook (`add_bda`, `add_gda`, `swap_atk_def_die`, `set_gda_zero_on_dmg`, `apply_status_on_hit_threshold`) now has dedicated isolation tests:
  - Registration assertion
  - Correct mutation of `AttackLoad`
  - Actor-guard check (hook ignores foreign actors)
- `test_attack_action_execution_flow` updated to use a programmatic template instead of `SkillN1`.
- Remaining AoE tests still load `"AreaAttack"` from `DataManager` (integration-level, acceptable per `ARCH.TEST_QUALITY.ATTACK_ACTION_DATA`).

### `.forbidden/architecture.md`
- **Created** `[ARCH.TEST_QUALITY.EFFECT_HOOKS]`: rule mandating programmatic templates for effect hook tests.
- **Updated** `[ARCH.TEST_QUALITY.ATTACK_ACTION_DATA]`: clarified that only integration-level skill tests may use `DataManager`; hook unit tests must use `[ARCH.TEST_QUALITY.EFFECT_HOOKS]`.
- **Created** `[ARCH.DOC.battle.BattleActions._build_add_bda]`: documentation for the new hook builder.
- **Updated** `[ARCH.DOC.battle.BattleActions.EFFECT_HOOK_BUILDERS]`: added `add_bda` to the registered builders list.

## Test Results
- **157 tests collected**, 156 passed, 1 pre-existing failure in `test_ForcaBruta.py` (unrelated to this task).
