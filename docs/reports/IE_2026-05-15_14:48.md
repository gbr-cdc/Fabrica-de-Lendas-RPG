# IE Report - Combo Passive Nerf
**Date:** 2026-05-15 14:48
**Task:** [WORKFLOWS.INDEPENDENT_EXECUTION] - Nerf Combo Passive

## Changes Implemented

### 1. Configuration (`data/BattlePassives.json`)
- Added `min_roll_for_second` parameter to the `Combo` passive, set to a default value of `5`.

### 2. Logic (`battle/BattlePassives.py`)
- Refactored the `Combo` class to simplify the extra attack logic.
- Removed `stage` and `hit` tracking state.
- Implemented `_executing_combo` flag to prevent infinite recursion during extra attacks.
- The passive now only triggers a second attack if:
    - The first attack hit.
    - The first attack's roll is greater than or equal to `min_roll_for_second`.
- The second attack is now a standard `AttackAction` (Basic Attack), which correctly generates focus for the character.
- Removed the third attack and stun mechanics.

### 3. Documentation
- Updated `[GDD.STYLES.LUTADOR.COMBO]` in `.forbidden/GDD.md` to reflect the new two-attack limit and roll threshold.
- Updated `[ARCH.DOC.battle.BattlePassives.Combo]` in `.forbidden/architecture.md` to match the implementation changes.

### 4. Tests (`tests/battle/test_Combo.py`)
- Updated the test suite to verify:
    - The roll threshold requirement for the second attack.
    - The hard limit of two attacks (no third attack).
    - Focus generation for the second attack.
    - Absence of stun effect.

## Verification Results
- All tests in `tests/battle/test_Combo.py` passed successfully.
- Manual inspection of action history confirmed focus generation and the removal of the third attack.

## Next Steps
- Request user to commit changes with message `IE: execution description`.
